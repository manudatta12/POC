{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('first_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="fos_user_change_password">
<h2 class="h2Heading"><span>R</span>Change Password</h2>
<div class="row">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
</div>
</form>
